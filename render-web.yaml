services:
  - type: web
    name: garden-landing-page-web
    env: node
  # Install dev dependencies as well (needed for Vite) and build
  buildCommand: npm ci --include=dev && npm run build
    startCommand: node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
